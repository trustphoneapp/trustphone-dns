<?xml version="1.0" encoding="utf-8"?>
<!--
  TrustPhone DNS keeps DNS query history, settings and rules on-device only.
  Cloud backup is fully disabled. Device-to-device transfer is allowed for
  settings but never copies the local query-log database.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="shield_dns.db" />
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
